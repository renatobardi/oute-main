services:
  postgres:
    image: postgres:15-alpine
    container_name: oute-postgres
    environment:
      POSTGRES_USER: app-user
      POSTGRES_PASSWORD: dev-password
      POSTGRES_DB: oute_db
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U app-user -d oute_db']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - oute-network

  design-system:
    build:
      context: .
      dockerfile: packages/design-system/Dockerfile
    container_name: oute-design-system
    ports:
      - '6006:6006'
    environment:
      - NODE_ENV=development
    networks:
      - oute-network

  99_home:
    build:
      context: .
      dockerfile: packages/99_home/Dockerfile
    container_name: oute-home
    ports:
      - '3003:3003'
    environment:
      - NODE_ENV=production
      - VITE_AUTH_SERVICE_URL=http://localhost:3001
      - VITE_API_TIMEOUT=30000
    networks:
      - oute-network

  00_dashboard:
    build:
      context: .
      dockerfile: packages/00_dashboard/Dockerfile
    container_name: oute-dashboard
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://app-user:dev-password@postgres:5432/oute_db
      - AUTH_SERVICE_URL=http://01_auth-profile:3001
      - PROJECTS_SERVICE_URL=http://02_projects:3002
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - oute-network

  01_auth-profile:
    build:
      context: .
      dockerfile: packages/01_auth-profile/Dockerfile
    container_name: oute-auth-profile
    ports:
      - '3001:3001'
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://app-user:dev-password@postgres:5432/oute_db
      - JWT_SECRET=dev-secret-key-change-in-production
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - oute-network

  02_projects:
    build:
      context: .
      dockerfile: packages/02_projects/Dockerfile
    container_name: oute-projects
    ports:
      - '3004:3002'
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://app-user:dev-password@postgres:5432/oute_db
      - AUTH_SERVICE_URL=http://01_auth-profile:3001
      - JWT_SECRET=dev-secret-key-change-in-production
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - oute-network

  03_interview:
    build:
      context: .
      dockerfile: packages/03_interview/Dockerfile
    container_name: oute-interview
    ports:
      - '3002:3002'
    environment:
      - NODE_ENV=production
      - VITE_AUTH_SERVICE_URL=http://localhost:3001
      - VITE_API_TIMEOUT=30000
    networks:
      - oute-network

volumes:
  postgres-data:
    driver: local

networks:
  oute-network:
    driver: bridge
